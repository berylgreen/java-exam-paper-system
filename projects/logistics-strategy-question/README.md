# 接口与策略模式 (物流系统)\n\n物流系统在处理 包裹 的不同状态或业务类型时，写死了冗长的 `switch-case` 逻辑。每次新增业务类型都需要修改核心类代码。\n\n**任务要求**：\n1. 引入策略模式，定义一个 `PackageStrategy` 接口。\n2. 为每一种业务分支创建一个实现了该接口的具体策略类。\n3. 在上下文中动态注入对应的策略实例来替换 `switch-case`。\n