# 多态与继承 (酒店系统)

当前酒店系统中存在一个庞大的统计类，里面使用了一连串的 `if (obj instanceof X) ... else if (obj instanceof Y) ...` 来分别处理不同类型的 房间 对象。

**任务要求**：
1. 定义一个通用的 `Room` 接口或抽象类，包含抽象方法。
2. 创建具体的子类实现各自的特有逻辑。
3. 利用多态重构统计类，消除所有的类型判断。
