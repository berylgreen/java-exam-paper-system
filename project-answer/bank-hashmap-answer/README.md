# 映射字典 (HashMap) (银行系统)

银行系统原先使用 `List` 存储账户信息。由于根据账户 ID 查询时需要遍历列表，当数据量增大时性能较差。

**任务要求：**
1. 使用 `HashMap<String, Account>` 重构账户存储结构，其中 Key 为账户 ID，Value 为对应的 `Account` 对象。
2. 实现以下功能：
   - 添加账户信息；
   - 根据账户 ID 查询账户；
   - 根据账户 ID 删除账户。
3. 编写 `Main` 测试类，演示上述功能，并体现使用 `HashMap` 后可通过键直接访问元素，从而提高查询效率。