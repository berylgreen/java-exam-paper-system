# 映射字典 (HashMap) (教务系统)\n\n教务系统目前使用两个平行的 `List` 分别存储 ID 和 学生 对象，当根据 ID 查询记录时需要进行 $O(N)$ 的遍历，性能极低。\n\n**任务要求**：\n1. 引入 `HashMap` 来替代平行列表，Key 使用 ID，Value 使用 `Student` 对象。\n2. 实现数据存入、根据 ID 高效获取及根据 ID 移除的功能。\n3. 编写 `Main` 测试用例验证 HashMap 带来的 $O(1)$ 查找便利。\n