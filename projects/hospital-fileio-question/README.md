# 文件输入输出 (I/O) (医疗系统)

医疗系统急需一个功能，用于将当天所有的 病患 操作流转记录持久化到本地的 `.txt` 文件中，目前只有一个空的方法占位。

**任务要求**：
1. 使用 `FileWriter` 和 `BufferedWriter` 将记录按行写入指定文件。
2. 正确处理产生的 `IOException` 异常。
3. 使用 `try-with-resources` 语法或者 `finally` 块确保在各种情况下流资源都被安全释放。
