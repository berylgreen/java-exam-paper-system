# 映射字典 (HashMap) (酒店系统)

酒店系统目前使用两个平行的 `List` 分别存储 ID 和 房间 对象，当根据 ID 查询记录时需要进行 $O(N)$ 的遍历，性能极低。

**任务要求**：
1. 引入 `HashMap` 来替代平行列表，Key 使用 ID，Value 使用 `Room` 对象。
2. 实现数据存入、根据 ID 高效获取及根据 ID 移除的功能。
3. 编写 `Main` 测试用例验证 HashMap 带来的 $O(1)$ 查找便利。
