# 异常处理 (餐饮系统)\n\n餐饮系统在批量解析外部的 菜品 数据时，一旦遇到格式错误，就会抛出未捕获的 `RuntimeException` 导致整个系统崩溃。\n\n**任务要求**：\n1. 自定义一个受检异常 `DishException`。\n2. 修改解析逻辑，使用 `try-catch` 结构捕获可能引发崩溃的地方。\n3. 在异常发生时打印错误日志并记录，然后继续解析下一个数据而不中断整体流程。\n