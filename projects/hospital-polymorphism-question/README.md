# 多态与继承 (医疗系统)\n\n当前医疗系统中存在一个庞大的统计类，里面使用了一连串的 `if (obj instanceof X) ... else if (obj instanceof Y) ...` 来分别处理不同类型的 病患 对象。\n\n**任务要求**：\n1. 定义一个通用的 `Patient` 接口或抽象类，包含抽象方法。\n2. 创建具体的子类实现各自的特有逻辑。\n3. 利用多态重构统计类，消除所有的类型判断。\n